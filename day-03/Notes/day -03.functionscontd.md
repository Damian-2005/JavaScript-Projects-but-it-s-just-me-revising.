So yeah, where did we stop. 

```
function learning(topic){
	console.log("Damian's learning" + topic);
}
```
The function written above is just me trying to recall what a function is. 

Not totally out of context:  But I recall installing node on my computer to be able to run JavaScript on the "server side" but I have barely tested it out yet.


I'll go give this code a run there

### What I picked up trying to run a code on the server side?

node -v and npm -v to check if node and npm packages are preinstalled already.

crtl + c to exit REPL mode or .exit

node (js filename) to run the code

