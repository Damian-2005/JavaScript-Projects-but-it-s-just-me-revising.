---
title: Array cardio continuation
desc: tackling code problems to understand array methods better.
---
[arrayName].forEach([function]) -> Takes the function and runs it through all the elements in the array, one by one.

#### Template Literal

Example:
This code
```
element + "(" + index + ")"
```
could be simply rewritten as:
```
`${element} (${index})`
```
using backticks - 

