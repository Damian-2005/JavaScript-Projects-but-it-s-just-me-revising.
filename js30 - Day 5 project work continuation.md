---
title: continuation of the flex grid display project
desc:
---
#### What am I learning today?

```
.panel > * -> this selects all the direct descendants of the panel class simply.
.panel >*:first-child-> this selects the first child of the direct child of the panel class elements, just the first child.
.panel.open-active --> this applies styles to the elements that have both panel and open-active tags
```

Transitions and Translations and cubic beiziers -- like 2x mini projects to master what these are. + A long list of array methods projects that I'll be doing today. 

-> .toggle() -> Adds a class to a selected or attached element if it doesn't exist there yet or if it does, it removes it. 

-> a little stroll down callback function memory lane

#### List of concepts to practice.

1. Array methods -> Map(), sort(), reduce() and filter
2. Transition, Translation, Cubic Biezier - intro to animation and all that.
3. Datasets.
4. _what does the forEach method even do?_
_What does the forEach method arrow syntax do?_
5. and CSS Variables.