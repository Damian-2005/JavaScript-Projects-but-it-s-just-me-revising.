---
title: Array Cardio Practice
desc: A bunch of examples to get better at arrays
---
### A more practical dive into Array Methods

### 1. Array.prototype.filter() 

Apparently all array methods take in functions, that is they can't be used without taking a function as a parameter. 

**Can't make sense of this yet.**

### 2. Array.prototype.map()

Takes in an array, does something to the inputted array and returns a new array of the same length.

### 3. Array.prototype.sort()

Takes an array, compares two values, return 1 if true and -1 if false till the entire array is sorted 

### 4. Array.prototype.reduce()

This array method let's us do the recurring math operations -- I mean, adding values of elements in an array that we normally use for and if loop to do.

Still haven't gotten it clear enough, so it makes the list of things to read on better at the end of today's grind.


